# Research & Precedent Engine (placeholder)

Future package. Natural-language legal search, related-precedent recommendations, judgment summarization, citation suggestions, legal-timeline extraction, similarity scoring.

Will share the module shape (`schemas.py` + `services/` + `views.py`). A shared corpus VDB may live at `data/outputs/_research/`; per-case query history under `data/outputs/<user_id>/<case_id>/research/`.
