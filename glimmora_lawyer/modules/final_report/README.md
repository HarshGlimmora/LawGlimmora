# Final Case Intelligence Report (placeholder)

Future package. Reads outputs from the other four packages and computes a deterministic scorecard: case strength, case weakness, pitch success estimate, evidence advantage/disadvantage, legal issue matrix, missing documentation list, strongest/risky authorities, recommended next actions, hearing readiness, mock-court outcome summary. One LLM call for the narrative executive summary. JSON + PDF export.

Outputs persist under `data/outputs/<user_id>/<case_id>/reports/`.
